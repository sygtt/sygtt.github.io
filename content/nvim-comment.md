---
title: neovimで素早くコメントアウトしたい
tags: 
    - neovim
---

## 1行のみコメントアウト

方法は2通りある。

1. Normalモードで `gcc` と入力。
2. `Shift + V` で1行まとめて選択して、 `gc` と入力。

<br>

## 複数行まとめてコメントアウト

`Shift + V` で複数行まとめて選択して、`gc` と入力。
